import { ScrollView, Text } from '@react-pakistan/react-native-commons-collection';
import styled from 'styled-components/native';

export const SpacexWrapper = styled(ScrollView)``;

export const SpacexHeading = styled(Text)``;
